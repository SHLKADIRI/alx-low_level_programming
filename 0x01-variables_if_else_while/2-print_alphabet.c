#include <stdio.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main()
{
	char letter;
	
	for (letter = 'a'; letter <= 'z'; letter++)
	{
		putchar(letter);
	}
	write("\n");
	return (0=;
}

