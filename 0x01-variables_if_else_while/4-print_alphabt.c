#include <stdio.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char l;

	while (l != 'q' && l != 'e'; l++)
	{
		putchar(l);
	}
	putchar('\n');
	return (0);
}

