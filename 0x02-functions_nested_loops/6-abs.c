#include "main.h"

/**
 * _abs - function that checks if a charcater 
 * Return: 1 is n is positive
 * and -1 if it's negative
 */
int _abs(int)
{
	int n;

	if (n > 0)
	{
		return (a);
	}
	else if (n < 0)
	{
		return (-a);
	}
}
