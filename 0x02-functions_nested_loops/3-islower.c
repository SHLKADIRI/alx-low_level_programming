#include "main.h"

/**
 * print_alphabet - function that prints alphabet from a to z
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	if (i >= 'a' && i <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
